{
  "name": "marea",
  "private": true,
  "version": "0.1.0",
  "description": "Marea — a browser-based isometric multiplayer luxury-harbor RPG on Solana.",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace shared",
    "genmap": "npm run genmap --workspace shared",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "dev": "npm run build:shared && concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build:shared && npm run build --workspace server && npm run build --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}
