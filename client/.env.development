# Dev only: the Vite dev server runs on :5173 while the Colyseus server runs on
# :2567, so the client must be told where to connect. In production this file is
# NOT used and the client connects same-origin (served by the server via nginx).
VITE_SERVER_URL=ws://localhost:2567
